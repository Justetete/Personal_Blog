mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml

server:
  port: 8081

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    name: my-blog-datasource
    password: Just1996 # mysql data password
    url: jdbc:mysql://localhost:3306/myblog_db?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false&serverTimezone=UTC
    username: root

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
